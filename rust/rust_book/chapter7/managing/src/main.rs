// * is the "Glob"-operator meaning everything public in that path
use std::collections::*;

// Nested path
use std::{cmp::Ordering, io};

fn main() {
    let mut map = HashMap::new();
    map.insert(1, 2);
}
