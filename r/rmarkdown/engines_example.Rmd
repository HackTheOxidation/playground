---
title: "Engines example"
output: pdf_document
date: "`r Sys.Date()`"
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```


```{r test-haskell, engine='haskell', engine.path='/home/hacker/.ghcup/bin/runghc', cache=TRUE}
module TestModule (f) where

f = [x | x <- [1..10], odd x]

main :: IO ()
main = do
        putStrLn (show f)
```

```{sage test-sage, engine='sage', engine.path='/usr/bin/sage', cache=TRUE}
print("hello")
x, y, C = var('x y C')
eq = x^5 - x^4*y^3 - 5*y == C
solve(eq, y)
```
